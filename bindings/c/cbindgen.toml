# cbindgen configuration for the mcr C ABI.
#
# Regenerate the committed header after changing the public FFI surface:
#
#     cbindgen --config cbindgen.toml --crate mcr-c --output include/mcr.h
#
# (Run from this `bindings/c/` directory. Install cbindgen with
# `cargo install cbindgen` if it is not on PATH.)

language = "C"
header = """/*
 * mcr.h — C ABI for the mcr chess rules library.
 *
 * GENERATED by cbindgen from bindings/c/src/lib.rs. Do not edit by hand;
 * regenerate with:
 *     cbindgen --config cbindgen.toml --crate mcr-c --output include/mcr.h
 *
 * See the crate docs (and the doc comments below) for ownership, the two-call
 * buffer contract, and panic safety.
 */"""
include_guard = "MCR_H"
pragma_once = false
cpp_compat = true
tab_width = 4
style = "type"
documentation = true
documentation_style = "doxy"

[parse]
parse_deps = false

[export]
include = ["McrPosition", "McrFairyPosition", "McrOutcome", "McrStatus"]
prefix = ""

[enum]
prefix_with_name = true
rename_variants = "ScreamingSnakeCase"

[fn]
sort_by = "None"
